export class User {
    constructor({ id, active, balance, avatar, firstName, lastName, gender }) {
        this.id = id;
        this.active = active;
        this.balance = balance;
        this.avatar = avatar;
        this.firstName = firstName;
        this.lastName = lastName;
        this.gender = gender;
    }
}
